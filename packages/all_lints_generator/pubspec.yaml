name: all_lints_generator
description: Regenerates analysis_options YAML files for the all_lints package.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
  yaml: ^3.1.0
